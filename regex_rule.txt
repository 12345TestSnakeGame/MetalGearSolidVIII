正则文法的规则：
第一行：所有的非终结符
第二行：所有的终结符
剩余行：所有的正则文法

非终结符只能是一个字符，如A,B,1,2，empty是例外，表示空串
非终结符，终结符之间互相用空格隔开，连在一起的会被视为一个符号
在后的正则文法会覆盖前面的正则文法，解决了关键字的问题（比如说if的生成式写在id后面，就会覆盖，然后if就不会被识别为id了）
右部中出现的非终结符必须在前面的行出现过
由'#'开头的所有行都会忽略掉，作为注释
由'^'开头的行，其符号只作为变量存在，方便复杂正则表达式的书写。而不能作为接收状态，比如digit
如果与正则表达式的符号冲突，如括号，|，在前面加个反斜杠\